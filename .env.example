# Base URL for the Backend API.
# Leave blank to automatically fallback to `${window.location.origin}/api/v1` when deployed,
# or set to your live backend server URL (e.g., https://api.yourdomain.com/api/v1).
VITE_API_URL=http://127.0.0.1:8000
